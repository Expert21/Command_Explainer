# Command Explainer Configuration

ollama:
  host: "http://localhost:11434"
  model: "dolphin-mistral:7b"
  timeout: 120  # seconds

personas:
  default: "general"
  available:
    - general
    - security

# Future: LoRA model configurations
# models:
#   security-lora:
#     name: "security-finetuned"
#     persona: security
